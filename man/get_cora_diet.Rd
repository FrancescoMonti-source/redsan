% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cora-diet.R
\name{get_cora_diet}
\alias{get_cora_diet}
\title{Retrieve CORA Diet observations by IEP or EVTID}
\usage{
get_cora_diet(
  ids,
  id_type = c("IEP", "EVTID"),
  event_type = c("H", "R"),
  connection = NULL,
  ojdbc_jar = NULL,
  chunk_size = 2000L,
  env = "edsan-ct",
  ks_path = NULL
)
}
\arguments{
\item{ids}{Character vector of stay identifiers.}

\item{id_type}{Input identifier type: \code{"IEP"} (default) or \code{"EVTID"}.}

\item{event_type}{CORA event types to retrieve. The default \code{c("H", "R")}
returns both hospital-linked (\code{"H"}) and rendez-vous-linked (\code{"R"})
Diet documents. Use \code{"H"} or \code{"R"} to restrict the lookup.}

\item{connection}{Optional existing CORA DBI connection. When \code{NULL},
\code{redsan} opens a CORA JDBC connection and closes it before returning.}

\item{ojdbc_jar}{Optional path to an Oracle JDBC driver. Used only when
\code{connection} is \code{NULL}.}

\item{chunk_size}{Maximum number of BLOB bytes requested per Oracle
\code{DBMS_LOB.SUBSTR()} call. The default (2000) is deliberately conservative
for Oracle RAW conversion through RJDBC.}

\item{env}{EDSaN CT web-service environment used for EVTID/IEP mapping.}

\item{ks_path}{Optional d2imr keystore path for EDSaN CT correspondence.}
}
\value{
A tibble with one row per active Diet document. The first two columns,
\code{EVTID} and \code{IEP}, expose the identifier correspondence; \code{TEXT} contains
the decompressed observation.
}
\description{
Retrieves Diet observation documents associated with one or more stays.
Input identifiers may be real hospital IEPs or pseudonymized EDSaN EVTIDs.
Both identifiers are returned in the output so their correspondence is
preserved alongside the CORA document.
}
\details{
Diet documents are identified in \code{T_DOCUMENT} by \code{NOSOUSVOLET = 443}
and active documents by \code{ETATDOC = 1}. By default both validated CORA
event paths are queried. Hospital documents (\code{TYPEEVT = "H"}) are reached
through \code{MVTUS.NOSEJ = IEP} and
\code{MVTUS.NOMVTUS = T_DOCUMENT.NOEVT}. Rendez-vous documents
(\code{TYPEEVT = "R"}) are reached through \code{T_EVT_RDV.NOSEJ = IEP} and
\code{T_EVT_RDV.NOEVT = T_DOCUMENT.NOEVT}. The \code{event_type} argument can
restrict retrieval to either path.

\code{T_DOCUMENT.NOEVT} is a CORA-internal event key and is returned as
\code{CORA_NOEVT} to avoid confusion with the EDSaN \code{EVTID}.

For both event types, the document payload is retrieved from
\code{T_DOCUMENT_MEMO.DOCUMENT_BRUT} using the pair
\code{(NODOCUMENT, TYPEDOC)} and decompressed from GZIP.

EVTID input is reidentified to IEP through EDSaN CT before the CORA lookup.
IEP input is pseudonymized through EDSaN CT so the corresponding EVTID can be
returned. A valid IEP with no EDSaN correspondence can still produce CORA rows
with \code{EVTID = NA}; a CT backend failure raises an error.

The function uses identifier/document-key lookups and does not scan
\code{T_DOCUMENT} or \code{T_DOCUMENT_MEMO} for text.
}

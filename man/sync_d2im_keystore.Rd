% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/d2im-keystore-sync.R
\name{sync_d2im_keystore}
\alias{sync_d2im_keystore}
\title{Synchronize missing D2IM keystore entries from Python to R}
\usage{
sync_d2im_keystore(python = NULL)
}
\arguments{
\item{python}{Python interpreter containing the \code{d2im} package. When \code{NULL},
\code{PYKERNEL} is used if it points to an existing file; otherwise the Podsan
default \code{/opt/kernels/py3.14/bin/python} is used.}
}
\value{
Invisibly, a character vector containing only the names of keys added
to the R keystore. Secret values are never returned or printed.
}
\description{
Copies entries that exist in the Python D2IM keystore but are absent from the
active \code{d2imr} keystore. Existing R entries are never overwritten. Before any
write, the active R keystore is backed up with file mode \code{0600}.
}
\details{
This is an administrative compatibility helper for environments where the
Python D2IM keystore is maintained more actively than the R keystore. It is
independent of normal EDSAN or ICCA retrieval and is never called
automatically by \code{redsan}.

The function requires the optional packages \code{d2imr} and \code{reticulate}. It uses
the active path returned by \code{d2imr::get_activ_keystore_path()} and the
internal \code{d2imr} keystore load/save functions because the installed \code{d2imr}
API does not provide an exported whole-keystore synchronization operation.

Secret values are transferred in process memory only. \code{keystore.show()} is
captured inside Python solely to recover key names; its output is not emitted
to the R console. If \code{reticulate} has already initialized a different Python
interpreter in the current R session, restart the R session before calling
this function.
}
\examples{
\dontrun{
sync_d2im_keystore()
added <- sync_d2im_keystore()
added # names only; no secret values
}
}

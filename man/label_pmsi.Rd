% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmsi.R
\name{label_pmsi}
\alias{label_pmsi}
\title{Add reference labels to normalized PMSI tables}
\usage{
label_pmsi(pmsi)
}
\arguments{
\item{pmsi}{A list containing the \code{main}, \code{actes}, and \code{diag} data frames,
normally returned by \code{\link[=process_pmsi]{process_pmsi()}}.}
}
\value{
The input PMSI list with \code{CODEACTE_LABEL} added to \code{actes} and
\code{CODE_LABEL} added to \code{diag}. All other tables, columns, and rows are
preserved.
}
\description{
Enriches normalized PMSI \code{actes} and \code{diag} tables with the reference labels
distributed with \code{redsan}. It can be applied to older artifacts;
\code{\link[=process_pmsi]{process_pmsi()}} uses the same function for new outputs. The \code{main} table is
returned unchanged.
}
\details{
Acts are matched to the combined CCAM/CDAM reference by
\code{NOMENCLATURE + CODEACTE}, producing \code{CODEACTE_LABEL}. Diagnoses are matched
to the CIM-10 reference by \code{diag = CODE}, producing \code{CODE_LABEL}. Joins are
exact, preserve every PMSI row, and leave unmatched labels as \code{NA}. Existing
\code{CODEACTE_LABEL} and \code{CODE_LABEL} columns are refreshed from the packaged
references.
Older artifacts without \code{NOMENCLATURE} retain their acts with missing
nomenclature and procedure labels.
}
\examples{
raw <- list(list(
  PATID = "P1",
  EVTID = "E1",
  ELTID = "L1",
  DATENT = "2024-01-01",
  DATSORT = "2024-01-02",
  CODEACTE1 = "JKFA023",
  NOMENCLATURE1 = "CCAM",
  DALL = "01:E46"
))
normalized <- process_pmsi(raw)
labelled <- label_pmsi(normalized)
labelled$actes[c("NOMENCLATURE", "CODEACTE", "CODEACTE_LABEL")]
labelled$diag[c("diag", "CODE_LABEL")]

}

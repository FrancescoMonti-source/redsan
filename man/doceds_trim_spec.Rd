% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doceds-trim.R
\name{doceds_trim_spec}
\alias{doceds_trim_spec}
\title{Which trimming rules ran, and with which limits}
\usage{
doceds_trim_spec(remove_lab_tables = FALSE)
}
\arguments{
\item{remove_lab_tables}{Whether the optional pasted-laboratory-table family
is active. Defaults to \code{FALSE}, matching \code{\link[=trim_doceds_text]{trim_doceds_text()}}.}
}
\value{
A list: \code{package} and \code{version} identify the installed rules;
\code{digest}, \code{digest_algorithm}, and \code{digest_schema} identify the rules
themselves, derived from them rather than declared; \code{remove_lab_tables}
records whether the optional laboratory-table family was active;
\code{preamble_rule} and \code{boilerplate_rule} name the rule sets;
\code{preamble_limit_chars} is how early the letter date has to appear to be
treated as a frame boundary; \code{boilerplate_families} names every family in
the order they are applied; \code{inline_rules} gives the patterns of the two
rules that act inside a line rather than on whole ones, which have patterns
instead of family names; and \code{near_total_share} with \code{near_total_min_chars}
are the diagnostic that abandons a trim which matched essentially the whole
document.
}
\description{
Reports the identity and the thresholds of the rules \code{\link[=trim_doceds_text]{trim_doceds_text()}}
applies, so a caller can record what produced a trimmed text alongside the
text itself.
}
\details{
A trimmed document is not self-describing: two runs a year apart can differ
because the families changed, because a bound moved, or because neither did.
A saved result that records this alongside its numbers can be told apart from
one that cannot, which matters when the numbers are being compared over time.

Read it rather than copying the values. A consumer that keeps its own copy of
a rule name or a threshold reports what it believes ran, which is the same
thing as reporting nothing once the two drift apart.

\code{digest} is the field to compare, because it is the one nobody maintains. It
is derived from every pattern and threshold the trimmer holds, so a rule that
changes changes it whether or not anyone remembered to say so. It is a
SHA-256 digest of the canonical rule text's UTF-8 bytes, computed without R
serialization, so two machines running the same rules agree on it whatever
their locale. \code{preamble_rule}
and \code{boilerplate_rule} are names and are deliberately not versioned — a
version written into a string is a fact somebody has to remember, and its only
possible failure is the one that matters: staying put while the rules move.

What \code{digest} does \strong{not} cover is the code that applies the rules. A change
to how spans are merged or removed leaves it untouched; \code{version} is what
records that. Two runs agreeing on both agree about the whole trimming.
}
\examples{
spec <- doceds_trim_spec()
spec$boilerplate_rule
spec$boilerplate_families

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doceds-structure.R
\name{map_header_structure}
\alias{map_header_structure}
\title{Map header-like structure in document text}
\usage{
map_header_structure(
  data,
  text_col = "RECTXT",
  id_col = "ELTID",
  metadata_cols = intersect(c("PATID", "EVTID", id_col, "RECDATE", "RECTYPE", "SEJUM",
    "SEJUF"), names(data)),
  max_line_chars = 100L,
  min_header_chars = 5L,
  n_examples = 5L,
  noise_pattern = NULL
)
}
\arguments{
\item{data}{A data frame containing document text.}

\item{text_col}{Name of the column containing document text.}

\item{id_col}{Name of the source-element identifier column. Defaults to
\code{ELTID} for DOCEDS data.}

\item{metadata_cols}{Columns retained in line-level outputs. By default,
available DOCEDS provenance and context columns are retained:
\code{PATID}, \code{EVTID}, \code{id_col}, \code{RECDATE}, \code{RECTYPE}, \code{SEJUM}, and \code{SEJUF}.}

\item{max_line_chars}{Maximum length of a line to consider as a candidate.}

\item{min_header_chars}{Minimum length of a normalized candidate label.}

\item{n_examples}{Maximum number of distinct raw variants retained in the
\code{examples} column.}

\item{noise_pattern}{Optional regular expression applied after the minimum
length filter. When \code{NULL}, no additional candidate labels are removed.}
}
\value{
A list with:
\itemize{
\item \code{lines}: one row per source-text line, including the requested metadata;
\item \code{candidate_hits}: candidate labels observed in each document;
\item \code{candidate_lines_raw}: candidate frequencies and raw \code{examples};
\item \code{candidate_lines}: candidates after \code{min_header_chars} and optional
\code{noise_pattern} filtering;
\item \code{header_hits}: line-level occurrences of filtered candidates;
\item \code{doc_signatures}: candidate sequence for each document;
\item \code{signature_counts}: frequency of each document-level sequence.
}
}
\description{
Splits document text into lines and identifies short uppercase or
colon-terminated lines as header candidates. The function returns both
normalized candidate labels and their document-level sequences. It does not
assign clinical meaning to any label.
}
\examples{
documents <- tibble::tibble(
  PATID = c("P1", "P2"),
  EVTID = c("E1", "E2"),
  ELTID = c("L1", "L2"),
  RECTXT = c(
    "ANTÉCÉDENTS\nEXAMEN CLINIQUE:\nTexte 1",
    "ANTECEDENTS\nEXAMEN CLINIQUE:\nTexte 2"
  )
)

mapped <- map_header_structure(documents)
mapped$candidate_lines_raw
mapped$candidate_lines

}

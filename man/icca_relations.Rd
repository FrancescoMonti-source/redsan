% Generated by roxygen2: do not edit by hand
\name{icca_relations}
\alias{icca_relations}
\title{Inspect declared ICCA object relations}
\usage{
icca_relations(
  source = NULL,
  direction = c("both", "out", "in"),
  connection = NULL
)
}
\arguments{
\item{source}{Optional ICCA object as \code{schema.object}. When supplied, only relations touching that object are returned.}
\item{direction}{With \code{source}, return relations going \code{"out"}, coming \code{"in"}, or \code{"both"} directions.}
\item{connection}{Optional existing ICCA DBI connection.}
}
\value{
A tibble of relation edges.
}
\description{
Combines SQL Server foreign keys, which describe declared table-to-table column relations, with SQL expression dependencies, which identify the objects used to build views.
}
\details{
View dependencies identify which objects are used by a view but do not by themselves encode the exact join columns. When exact view join predicates matter, inspect the corresponding SQL view definition.
}

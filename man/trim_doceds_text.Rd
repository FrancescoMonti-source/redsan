% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doceds-trim.R
\name{trim_doceds_text}
\alias{trim_doceds_text}
\title{Remove the administrative frame from one DOCEDS document}
\usage{
trim_doceds_text(text, remove_lab_tables = TRUE)
}
\arguments{
\item{text}{One document's text, normally one \code{RECTXT} value. Length 0, \code{NA}
and \code{""} return the input unchanged with zeroed counts. A longer vector is
an error rather than a silent \code{NA}: map over it with \code{\link[=lapply]{lapply()}}.}

\item{remove_lab_tables}{Whether to remove recognised pasted laboratory
tables. Defaults to \code{TRUE}. Set \code{FALSE} when that source content must remain
visible.}
}
\value{
A list. \code{text} is what survives; \code{net_removed_chars} is the exact
difference in characters between the input and it, and is the \strong{only
total} in the list. Every other count is standalone — measured against its
own rule's matches over the whole document — so counts overlap each other
and must not be summed. A placeholder inside a letterhead is counted both
by \code{placeholders_standalone_chars} and by the family that cuts the
letterhead.

The remaining elements are \code{marker_count} and \code{marker_within_limit} for the
letter-date frame boundary; \code{preamble_removed}, \code{removed_prefix_chars} and
\code{removed_prefix_start} for the leading letterhead; \code{boilerplate_removed},
\code{boilerplate_removed_chars}, \code{boilerplate_families} and
\code{boilerplate_family_standalone_chars} for the block families;
\code{placeholders_standalone_chars} and \code{rule_runs_standalone_chars} for the
two inline rules; \code{removed_share}; \code{near_total_match_detected}; and two
span tables, \code{boilerplate_intervals} and \code{removed_intervals}. The latter is
everything actually removed, after protection and after the near-total
check, and is what an audit should read.
}
\description{
Removes the letterhead, the recognised boilerplate families, the Word field
residue and the fill-character runs from one document's text, and reports
exactly what it took.
}
\details{
The administrative rules remove letterheads, correspondence blocks, RGPD
notices, unfilled identity banners, page furniture, and the placeholders and
fill runs a Word template leaves behind. They are meant not to touch what a
clinician wrote, and the instruments under \verb{tools/} exist to keep checking
that boundary.

The optional \code{lab_table} family is different. With
\code{remove_lab_tables = TRUE} it removes recognised pasted laboratory tables,
which can contain clinician-authored clinical values. This is an explicit
evidence-scope policy, not part of the administrative-frame guarantee. It is
enabled by default and can be disabled by the caller.

Every rule contributes candidate spans in the coordinates of the \strong{original}
document rather than editing the string. Lines carrying a measured constant —
\code{TA : 130/80}, \verb{Poids : 144 kg} — are subtracted from those spans first, so a
family that swallowed a vital sign gives it back. What survives is applied in
a single pass, which is what makes the removals auditable, order-independent
and reportable as \code{removed_intervals}.

Each span carries the separator the text closes with: a newline for a block
cut, the space it occupied for an inline field, nothing for a fill run. When
spans merge, the widest separator wins.

\code{near_total_match_detected} is a \strong{diagnostic for one failure} — a rule that
ran away on a layout nobody has seen and matched essentially the whole
document — and not a safety margin. A document losing 99.4 percent is not
clinically different from one losing 99.6, so no guarantee about clinical
text rests on which side of it a document falls. When it fires, nothing is
removed and the original text is returned.

The rules are site-specific to the Rouen corpus they were measured against.
}
\examples{
letter <- paste(
  "CENTRE HOSPITALIER UNIVERSITAIRE",
  "Rouen, le 12 mars 2024",
  "Le patient decrit une dyspnee d'effort depuis trois semaines.",
  sep = "\n"
)
trimmed <- trim_doceds_text(letter)
trimmed$text
trimmed$net_removed_chars

}
\seealso{
\code{\link[=process_doceds]{process_doceds()}} for the table this text comes from.
}

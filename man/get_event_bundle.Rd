% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event-bundle.R
\name{get_event_bundle}
\alias{get_event_bundle}
\title{Retrieve normalized EDSAN sources for one event}
\usage{
get_event_bundle(event_id, modules = "all")
}
\arguments{
\item{event_id}{One non-missing EDSAN \code{EVTID}.}

\item{modules}{EDSAN modules to retrieve. Use \code{"all"} (the default) for every
registered module, or provide a character vector.}
}
\value{
One \code{edsan_event_bundle}: a list with \code{event_id}, \code{sources}, and
\code{created_at} entries. PMSI remains a list with \code{main}, \code{actes}, and \code{diag};
DOCEDS, BIOL, and VIRO remain normalized tibbles.
}
\description{
Singular convenience wrapper around \code{\link[=get_event_bundles]{get_event_bundles()}}. It retrieves each
selected module for one \code{EVTID} and returns the bundle directly rather than
inside a named collection.
}
\details{
This function only forwards to \code{\link[=get_event_bundles]{get_event_bundles()}} and unwraps the single
bundle, so retrieval, normalization, and reference labelling behave exactly as
in the plural form: \code{biol} carries \code{TYPEANA_LABEL} from \code{\link[=label_biol]{label_biol()}} and
PMSI carries the labels added by \code{\link[=label_pmsi]{label_pmsi()}}.

Retrieval is fail-fast. If any selected module cannot be retrieved, no
partial bundle is returned. Empty normalized module tables are retained.
}
\examples{
\dontrun{
bundle <- get_event_bundle("123456789")
bundle <- get_event_bundle(
  "123456789",
  modules = c("doceds", "pmsi", "biol")
)
}

}

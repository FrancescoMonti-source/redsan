% Generated by roxygen2: do not edit by hand
\name{get_icca}
\alias{get_icca}
\title{Retrieve ICCA data by EDSaN EVTID}
\usage{
get_icca(
  evtids,
  source = "encounter",
  link = "auto",
  connection = NULL,
  instance = c("adult", "ped"),
  env = "edsan-ct",
  ks_path = NULL
)
}
\arguments{
\item{evtids}{Character vector of EDSaN EVTID values.}

\item{source}{ICCA source. Use \code{"encounter"} for the encounter summary, or any ICCA table/view as \code{"schema.object"}, for example \code{"DAR.PatientVentilation"}. The aliases \code{"assessment"} and \code{"medication"} remain available as shortcuts for \code{"DAR.PtAssessment"} and \code{"DAR.PtMedication"}; they are not a whitelist.}

\item{link}{Linkage column used to connect the requested source to \code{D_Encounter}. With \code{"auto"}, \code{encounterId} is used when present. \code{patientId} or \code{episodeId} must be requested explicitly because they may retrieve data outside the requested stay.}

\item{connection}{Optional existing ICCA DBI connection.}

\item{instance}{ICCA instance to query: \code{"adult"} (default) or \code{"ped"}. Ignored when \code{connection} is supplied explicitly.}

\item{env}{EDSaN CT web-service environment name.}

\item{ks_path}{Optional d2imr keystore path for EDSaN CT correspondence.}
}
\value{
A tibble keyed by \code{EVTID}. For generic sources, source columns are returned as stored by that table/view.
}
\description{
Retrieves ICCA rows for pseudonymized EDSaN stay identifiers. \code{redsan} reidentifies each EVTID to its IEP only long enough to locate the corresponding ICCA encounter, then queries the requested ICCA source and reattaches the original EVTID.
}
\details{
\code{get_icca()} is not limited to a fixed list of clinical domains. Any ICCA table/view exposing a compatible direct \code{D_Encounter} linkage column can be requested by name. Use \code{icca_catalog()}, \code{icca_describe()}, and \code{icca_relations()} to discover and inspect sources. \code{query_icca()} remains the unrestricted read-only SQL interface, including for objects requiring indirect relation paths that \code{get_icca()} cannot yet resolve automatically.

Mappings returned by EDSaN CT are consumed directly. If CT returns several IEPs for one EVTID, all corresponding encounters are queried. EVTIDs without an IEP correspondence produce no ICCA rows. \code{character(0)} returns immediately and never produces an unfiltered query.

The IEP used by the EVTID-to-encounter bridge is transient and is not injected into the result. However, a requested ICCA source may itself contain identifying columns (for example some \code{DAR.Patient*} views); generic retrieval returns the selected source as-is rather than stripping such columns by design.

For the empirically studied \code{PtAssessment} and \code{PtMedication} structures, repeated rows sharing \code{cisPtInterventionId} behaved as attributes of the same intervention instance. \code{redsan} does not pivot, deduplicate, or clinically filter generic source rows.
}

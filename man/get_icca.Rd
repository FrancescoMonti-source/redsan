% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/icca.R
\name{get_icca}
\alias{get_icca}
\title{Retrieve ICCA data by EDSaN EVTID}
\usage{
get_icca(
  evtids,
  source = "encounter",
  connection = NULL,
  env = "edsan-ct",
  ks_path = NULL
)
}
\arguments{
\item{evtids}{Character vector of EDSaN EVTID values.}

\item{source}{ICCA source to retrieve. Currently only \code{"encounter"} is
implemented and validated.}

\item{connection}{Optional existing ICCA DBI connection, forwarded to
\code{\link[=query_icca]{query_icca()}}. A caller-supplied connection is never closed by
\code{redsan}.}

\item{env}{EDSaN CT web-service environment name.}

\item{ks_path}{Optional d2imr keystore path for EDSaN CT correspondence.}
}
\value{
A tibble keyed by \code{EVTID}. For \code{source = "encounter"}, the returned
fields are \code{EVTID}, \code{encounterId}, \code{patientId}, \code{episodeId}, \code{gender},
\code{primaryDiagnosis}, \code{isArchived}, and \code{systemId}.
}
\description{
Retrieves ICCA rows for pseudonymized EDSaN stay identifiers. \code{redsan}
reidentifies each EVTID to its IEP only long enough to filter ICCA, then
replaces the IEP with the original EVTID before returning data.
}
\details{
\code{get_icca()} consumes the EVTID-to-IEP correspondences returned by EDSaN CT
directly. If CT returns several IEPs for one EVTID, all are queried; EVTIDs
without an IEP correspondence simply produce no ICCA rows. \code{character(0)}
returns an empty result immediately and never produces an unfiltered query.

The encounter query deliberately does not request \code{lifeTimeNumber} (IPP),
\code{accountNumber}, patient names, or date of birth. \code{encounterNumber} (IEP) is
requested only as a transient join key and is removed before the result is
returned.
}
\examples{
\dontrun{
get_icca("357015848")
get_icca(c("357015848", "123456789"), source = "encounter")
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edsan-ct.R
\name{edsan_pseudonymize}
\alias{edsan_pseudonymize}
\title{Translate real hospital identifiers to EDSaN identifiers}
\usage{
edsan_pseudonymize(ids, id_type = NULL, env = "edsan-ct", ks_path = NULL)
}
\arguments{
\item{ids}{Real hospital identifiers. Must be character so IPP leading zeroes
cannot be lost.}

\item{id_type}{Optional explicit input type: \code{"IPP"} or \code{"IEP"}. Takes
precedence over automatic detection.}

\item{env}{EDSaN CT web-service environment name.}

\item{ks_path}{Optional d2imr keystore path. When \code{NULL}, the active path is
resolved and passed explicitly, avoiding the invalid default in some d2imr
versions.}
}
\value{
A tibble retaining every input and reporting matched, not-found, or
multiple correspondences. \code{not-found} is only reported for a valid EDSaN
CT response with no correspondence; a backend failure or error response
raises an error instead.
}
\description{
Uses EDSaN CT to translate patient identifiers (\code{IPP}) to \code{PATID} and stay
identifiers (\code{IEP}) to \code{EVTID}. When \code{id_type} is omitted, each identifier is
classified from the local invariant: IPP starts with \code{00}; every other digit
string is treated as IEP. An explicit \code{id_type} overrides this detection; it
only warns, and never errors, when a value does not match the heuristic.
}

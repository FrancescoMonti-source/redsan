% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmsi.R
\name{prefer_pmsi_src_c_over_dw}
\alias{prefer_pmsi_src_c_over_dw}
\title{Prefer PMSI source C over DW within each unit}
\usage{
prefer_pmsi_src_c_over_dw(main)
}
\arguments{
\item{main}{A PMSI main data frame. When \code{SRC} is present, \code{PATID}, \code{EVTID},
\code{SEJUM}, and \code{SEJUF} must also be present so the precedence group is
defined.}
}
\value{
\code{main} after applying the \code{C}-over-\code{DW} rule, or unchanged when
\code{SRC} is absent.
}
\description{
Applies the PMSI source rule used by \code{\link[=process_pmsi]{process_pmsi()}} by default.
Within each \code{PATID + EVTID + SEJUM + SEJUF} group, rows whose \code{SRC} is \code{"DW"}
are removed only when at least one \code{"C"} row is present. \code{"DW"} remains the
fallback for units without \code{"C"}; unknown and missing sources are always
retained. Source labels are matched case-insensitively after trimming, but
their stored values are not modified.
}
\details{
Rows with a missing \code{PATID} or \code{EVTID} are not subject to source precedence.
If \code{SRC} is absent, \code{main} is returned unchanged.
The function preserves the input columns, column types, and retained row
order, including \code{ELTID}.
}
\examples{
main <- tibble::tibble(
  PATID = c("P1", "P1", "P1"),
  EVTID = c("E1", "E1", "E1"),
  SEJUM = c("U1", "U1", "U2"),
  SEJUF = c("UF1", "UF1", "UF2"),
  SRC = c("DW", "C", "DW")
)
prefer_pmsi_src_c_over_dw(main)

}

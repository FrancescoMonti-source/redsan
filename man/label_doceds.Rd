% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doceds.R
\name{label_doceds}
\alias{label_doceds}
\title{Label DOCEDS document types}
\usage{
label_doceds(documents)
}
\arguments{
\item{documents}{A normalized documents data frame containing \code{RECTYPE},
normally returned by \code{\link[=process_doceds]{process_doceds()}}.}
}
\value{
The input documents data frame with \code{RECTYPE_LABEL} added, and
\code{RECTYPE} as character. All other columns and rows are preserved.
}
\description{
Adds the authoritative document-type label to a normalized DOCEDS table by
matching \code{RECTYPE} against the packaged \code{rectypes} reference.
}
\details{
The join is exact, preserves every document row and leaves unmatched labels
as \code{NA}. An existing \code{RECTYPE_LABEL} column is refreshed from the
packaged reference. Several \code{rectypes} entries are documented with an
empty label in the source system; those keep \code{NA} rather than being
dropped.

A \code{RECTYPE} column that raw EDSAN payloads expose as a one-element list
per row is flattened to character before matching, since the reference key
must be atomic. Values holding several codes are collapsed with \verb{;}, and
empty values become \code{NA}.
}
\examples{
documents <- data.frame(
  ELTID = c("L1", "L2"),
  RECTYPE = c("AJ", "PAS_UN_TYPE")
)
labelled <- label_doceds(documents)
labelled[c("RECTYPE", "RECTYPE_LABEL")]

}

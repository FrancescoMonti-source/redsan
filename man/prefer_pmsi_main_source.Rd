% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmsi.R
\name{prefer_pmsi_main_source}
\alias{prefer_pmsi_main_source}
\title{Prefer the authoritative PMSI main source within each unit}
\usage{
prefer_pmsi_main_source(main)
}
\arguments{
\item{main}{A PMSI main data frame containing \code{PATID}, \code{EVTID}, \code{SEJUM},
\code{SEJUF}, and \code{SRC}.}
}
\value{
\code{main} restricted to the source-preferred rows.
}
\description{
Builds a source-preferred view of a complete \code{process_pmsi()} \code{main} table.
Within each \code{PATID + EVTID + SEJUM + SEJUF} group, rows whose \code{SRC} is \code{"DW"}
are removed only when at least one \code{"C"} row is present. \code{"DW"} remains the
fallback for units without \code{"C"}; unknown and missing sources are always
retained. Source labels are matched case-insensitively after trimming, but
their stored values are not modified.
}
\details{
Rows with a missing \code{PATID} or \code{EVTID} are not subject to source precedence.
The function preserves the input columns, column types, and retained row
order, including \code{ELTID}.
}
\examples{
main <- tibble::tibble(
  PATID = c("P1", "P1", "P1"),
  EVTID = c("E1", "E1", "E1"),
  SEJUM = c("U1", "U1", "U2"),
  SEJUF = c("UF1", "UF1", "UF2"),
  SRC = c("DW", "C", "DW")
)
prefer_pmsi_main_source(main)

}

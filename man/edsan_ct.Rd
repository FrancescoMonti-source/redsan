% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edsan-ct.R
\name{edsan_ct}
\alias{edsan_ct}
\title{Resolve EDSaN CT identifier correspondence}
\usage{
edsan_ct(ids, from, identity = FALSE, env = "edsan-ct", ks_path = NULL)
}
\arguments{
\item{ids}{One or more identifiers as character strings. Character input is
required to preserve leading zeroes and values outside safe floating-point
precision.}

\item{from}{Source identifier type: \code{"IPP"}, \code{"IEP"}, \code{"PATID"}, or
\code{"EVTID"}.}

\item{identity}{One non-missing logical value. If \code{TRUE}, also resolve the
patient identity associated with the direct correspondence.}

\item{env}{EDSaN CT web-service environment name.}

\item{ks_path}{Optional d2imr keystore path. When \code{NULL}, the active path is
resolved by d2imr.}
}
\value{
A tibble containing the source and destination identifier columns,
\code{status}, and \code{n_matches}. With \code{identity = TRUE}, patient identifiers and
identity fields are appended without changing the direct-match metadata.
}
\description{
Translates one explicit identifier type through the EDSaN correspondence
service. The direction is determined entirely by \code{from}: \code{IPP} resolves to
\code{PATID}, \code{PATID} to \code{IPP}, \code{IEP} to \code{EVTID}, and \code{EVTID} to \code{IEP}.
}
\details{
Results use the semantic identifier names so they can be joined directly to
source data. A valid response without a correspondence is retained with
\code{status = "not_found"} and \code{n_matches = 0}; service and response failures are
errors.
}
